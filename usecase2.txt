def newUser(emailEntry, passwdEntry, codeEntry):
	input(email)
	input(passwd)
	while email exist do
		print("email exist, insert new email)
	end while
	if (email not exist) and (passwd.length more than 8)
		send code to email 
	end if
	else
		input(code)
	while code not same do
		print("code invalid")
	end while
	if code same
		save user to database
		print("Registration success")
	end if
	 